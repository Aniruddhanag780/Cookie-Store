{
  "entities": {
    "UserAccount": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserAccount",
      "type": "object",
      "description": "Represents a user account within the AnimEcom application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user account."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "First name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "address": {
          "type": "string",
          "description": "User's address."
        },
        "orderIds": {
          "type": "array",
          "description": "References to Orders. (Relationship: UserAccount 1:N Order)",
          "items": {
            "type": "string"
          }
        },
        "cart": {
          "type": "array",
          "items": {
            "$ref": "#/backend/entities/CartItem"
          }
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName"
      ]
    },
    "Image": {
      "title": "Image",
      "type": "object",
      "description": "Represents an image with its URL and metadata.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the image."
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of the image."
        },
        "description": {
          "type": "string",
          "description": "A description of the image for accessibility."
        },
        "imageHint": {
          "type": "string",
          "description": "AI hint for the image."
        }
      },
      "required": [
        "id",
        "imageUrl"
      ]
    },
    "ContactMessage": {
      "title": "ContactMessage",
      "type": "object",
      "description": "Represents a message submitted through the contact form.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the person submitting the message."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the person."
        },
        "message": {
          "type": "string",
          "description": "The content of the message."
        },
        "submittedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the message was submitted."
        }
      },
      "required": [
        "name",
        "email",
        "message",
        "submittedAt"
      ]
    },
    "NewsletterSubscription": {
      "title": "NewsletterSubscription",
      "type": "object",
      "description": "Represents an email subscribed to the newsletter.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the subscriber."
        },
        "subscribedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the subscription was made."
        }
      },
      "required": [
        "email",
        "subscribedAt"
      ]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a product in the store.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "slug": { "type": "string" },
        "description": { "type": "string" },
        "longDescription": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string" },
        "image": { "type": "string" },
        "imageHint": { "type": "string" }
      },
      "required": ["id", "name", "slug", "price", "image"]
    },
    "Order": {
      "title": "Order",
      "type": "object",
      "description": "Represents a customer order.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["Processing", "Shipped", "Delivered"] },
        "total": { "type": "number" },
        "items": {
          "type": "array",
          "items": { "$ref": "#/backend/entities/Product" }
        }
      },
      "required": ["id", "userId", "date", "status", "total", "items"]
    },
    "CartItem": {
      "title": "CartItem",
      "type": "object",
      "description": "Represents an item in the user's shopping cart.",
      "properties": {
        "id": { "type": "string" },
        "quantity": { "type": "number" }
      },
      "required": ["id", "quantity"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "microsoft.com",
      "google.com",
      "github.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserAccount",
          "schema": { "$ref": "#/backend/entities/UserAccount" },
          "description": "Stores user account information. Path-based ownership ensures only the authenticated user can access their own data.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user, matching the Firebase Auth UID." }
          ]
        }
      },
      {
        "path": "/users/{userId}/cart/{productId}",
        "definition": {
            "entityName": "CartItem",
            "schema": { "$ref": "#/backend/entities/CartItem" },
            "description": "Stores items in a user's shopping cart. Only the user can read/write their own cart.",
            "params": [
                { "name": "userId", "description": "The UID of the user." },
                { "name": "productId", "description": "The ID of the product in the cart." }
            ]
        }
      },
      {
        "path": "/Image/{imageId}",
        "definition": {
          "entityName": "Image",
          "schema": { "$ref": "#/backend/entities/Image" },
          "description": "Stores image URLs for the application, like the hero banner.",
          "params": [{ "name": "imageId", "description": "The unique identifier of the image." }]
        }
      },
      {
        "path": "/contactus/{contactId}",
        "definition": {
          "entityName": "ContactMessage",
          "schema": { "$ref": "#/backend/entities/ContactMessage" },
          "description": "Stores messages submitted through the contact form. Publicly writable but not readable by clients.",
          "params": [{ "name": "contactId", "description": "The unique identifier for the contact message." }]
        }
      },
      {
        "path": "/newsletterSubscriptions/{subscriptionId}",
        "definition": {
          "entityName": "NewsletterSubscription",
          "schema": { "$ref": "#/backend/entities/NewsletterSubscription" },
          "description": "Stores email addresses for newsletter subscriptions. Publicly writable but not readable by clients.",
          "params": [{ "name": "subscriptionId", "description": "The unique identifier for the subscription." }]
        }
      },
      {
        "path": "/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": { "$ref": "#/backend/entities/Order" },
          "description": "Stores customer orders. Users can only read their own orders.",
          "params": [{ "name": "orderId", "description": "The unique identifier of the order." }]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure Authorization Independence, DBAC, and QAPs. User accounts are stored under `/users/{userId}`. This path-based ownership ensures that only the authenticated user can access their own data. The `Image` collection is public to allow anonymous users to view images like the hero banner. The `contactus` collection is publicly writable so anyone can submit a message, but client-side reads are disabled to protect submitter privacy. The `orders` collection is readable by users only if the `userId` on the order document matches their own UID."
  }
}
